import React from 'react';
import ReactDOM from 'react-dom/client';
import Scene from './3d-golden-ring.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Scene />
  </React.StrictMode>
);